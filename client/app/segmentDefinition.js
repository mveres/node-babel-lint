import React from 'react';

export default class relays extends React.Component {
  render = () =>
    <div>
      <button onClick={ () => {} }>
        <i className={ 'fa fa-plus' } />
      </button>
    </div>;
}
